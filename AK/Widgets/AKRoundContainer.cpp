#include <AK/Widgets/AKRoundContainer.h>

using namespace AK;

void AKRoundContainer::onBake(SkCanvas *canvas, const SkRegion &clip, bool surfaceChanged)
{
    /*
    AKSubScene::onBake(canvas, clip, surfaceChanged);
    const SkRegion &sceneDamage { damage() };
    SkRegion opaqueRegion { this->opaqueRegion() };

    */
}
