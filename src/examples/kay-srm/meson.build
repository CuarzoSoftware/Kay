srm_dep = dependency('SRM')
egl_dep = dependency('egl')

executable(
    'kay-srm',
    sources : ['main.cpp'],
    dependencies : [
        kay_dep,
        srm_dep,
        egl_dep
    ],
    install : true)
